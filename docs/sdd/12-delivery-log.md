# Delivery Log

A running, dated record of what was actually delivered. This is the honest history of
the project: commitments made, work completed, work cut, and rework required.

It exists for two reasons. Internally, it makes the difference between planned and
actual visible while there is still time to react. Externally, it is the evidence for
anything that asks what was delivered and at what cost.

## Session log

| Date | Session focus | Committed | Delivered | Cut or deferred | Rework | Notes |
|---|---|---|---|---|---|---|
| | | | | | | |

## Story completion record

| Story | Specified | Planned | Implemented | Reviewed | Done | Elapsed |
|---|---|---|---|---|---|---|
| US-001 | ☑ | ☑ | — | — | — | spec migrated; `007` builds it |
| US-002 | | | | | | |
| US-005 | | | | | | |
| US-006 | | | | | | |
| US-007 | | | | | | |
| US-008 | | | | | | |
| US-010 | | | | | |
| US-014 | | | | | | |

## Rework register

Rework is not a failure to hide; it is the most useful signal in the log. Each entry
records what had to be redone and what would have prevented it.

| Date | What was reworked | Root cause | What would have prevented it |
|---|---|---|---|
| | | | |

## Estimate accuracy

Recorded per story so that later estimates improve within the same project.

| Story | Estimated | Actual | Variance | Why |
|---|---|---|---|---|
| | | | | |

## Scope decisions

Every decision to cut, defer, or add scope, with the reason and the moment it was
made. A cut recorded at the time it happened is a planning decision; the same cut
noticed at the end is an incomplete feature.

| Date | Decision | Reason |
|---|---|---|
| | Deferred US-011, US-012, US-013 out of Release 1 | See `08-board.md` — no live provider in scope, and channel is a field rather than a feature |
| | Added localization to core scope; infrastructure moved into the walking skeleton | Retrofitting i18n costs far more than building with it. See `decisions/ADR-007-localization.md` |
| | Added US-014 to Release 1 and named US-006 as the first thing to cut if time runs short | Release 1 grew from seven stories to eight; the cut line moved rather than the estimate being ignored |
| | Confirmed React; closed Q-4 and Q-12 | The Angular argument from ADR-009 is understood and the trade is accepted: if an Angular component library exists, none of it is reusable and all eight primitives are rebuilt. Recorded in ADR-003 rather than left implicit |
| | Proposed ADR-011: React architecture — no global store, URL as state container, feature folders, fetching only at route level | Server state belongs to TanStack Query; the client state that remains is small enough that a store would have nothing to hold |
| | Proposed ADR-010: vertical slices over a thin domain core, amending ADR-002's four-project layout | With the system fully specified, the coupling between features turned out to be near zero, which is what layering protects against. Flagged the convention risk explicitly — a conventional structure understood beats an optimal one defended weakly |
| | Broke the build into seven phases, each ending in something demonstrable | Ordered by cost-of-delay rather than by ease. The rule that resolves the cross-cutting tension: add a concern when there is exactly one consumer — zero is speculative, seven is a retrofit, one is free |
| | Specified sidebar collapse as three states with a flyout for the nested item | Collapse is usually treated as binary, which leaves nested children unreachable and drops the sidebar entirely on mobile. Also recorded the width-animation exception rather than breaking rule 19 silently |
| | Added US-016 Dashboard to Release 2 with a full screen spec | Fifth scope addition, and off the critical demo path. The design principle is recorded: lead with what needs action, not with totals — the test being whether a changing number causes someone to behave differently |
| | Approved eight mark treatments with a slot assignment for each | Each changes exactly one variable — weight, container, colour, or orientation — never the geometry. Two variables at once produces a second mark, and a library of marks is not a system |
| | Extended the identity beyond the mark: pattern, converge loader, empty-state vocabulary, accent role, and a bilingual product glossary | A mark that only appears as a logo is not an identity. All five derive from the same geometry, so nothing new is invented. The loader matters most — it is seen far more often than the logo |
| | Gave teal a defined role: presence, never state; green stays status, never brand | Two colours with no rule is how a palette drifts, and confusing "resolved" with "online" makes both meaningless |
| | Replaced the ray burst with an interactive neural mesh and desaturated the whole panel | Rays were decoration pointing at a centre; the mesh is the idea. The palette had drifted to near-neon violet — high saturation everywhere with glow on everything is what reads as machine-generated, and the fix was subtraction |
| | Chose a contact shadow at the seam over a hard edge or a blend | A gradient between `#12121F` and white produces a dead grey band that exists nowhere else in the system, and lets the panel's noise bleed into the calm half. Rule recorded in `design/DESIGN-BRIEF.md` 21 |
| | Removed the login card; form sits directly on white | An elevated card on a dark background was separating the form from nothing, and cost a shadow, a radius, and a border |
| | Settled the mark as **Converge** — three threads arriving at one node — after two rejected iterations | Both earlier attempts looked right at 48px and failed at 20: one was the standard hyperlink glyph, the other read as a star. Rule recorded: judge a mark at 20px and ask what it resembles before asking whether it is good. Second lesson: when a mark reads as the wrong thing, change the construction, not the count |
| | Rebuilt the login screen spec around the channel network, with a separately designed mobile layout | Mobile was initially a scaled desktop: nodes overlapped into a cross and the headline was hidden. Recorded as a rule — a design that is shrunk breaks; a design is re-laid-out |
| | Named the product **Wasl · وصل** and drew three candidate marks | وصل means both "connection" and "receipt" — the two things the product is. Availability not verified; must be checked before the name goes anywhere public. See `design/brand.md` |
| | Wrote `design/motion.md`: expressive motion confined to login and empty states, 300ms ceiling everywhere else | A marketing site and a working tool have opposite motion requirements. Motion that impresses on visit one is friction on visit four hundred |
| | Reversed the logo-upload exclusion in ADR-012; schema, endpoints, and validation designed in `design/settings-and-uploads.md` | The original reasoning generalised from ticket attachments, which is a different risk profile. Images stored as `bytea` — the right call for three small images, the wrong one at scale, with the migration path recorded |
| | Proposed ADR-012: tenant theming — architecture in the skeleton, settings screen deferred to Release 2 | The token structure already supports it; the hard parts are the derived ramp, the computed foreground, and keeping status colours out of the themeable set. Fourth scope addition — the settings screen is the part to decline |
| | Wrote a full spec per screen in `design/screens/` — ten files covering every element, action, and state | A Figma frame shows one state; a screen has five or six, and the skipped ones are always empty, error, forbidden, and conflict. These bind each element to a token and each action to an endpoint with its failure paths |
| | **Dropped the aperture rule after seeing it at 18px.** Signature is now corner radius 2 plus a tighter 16-unit keyline — felt, not seen | At 18px an interrupted contour reads as a rendering defect or a disabled state, taxes the eye across a dense set, and can collide with meaning (an open padlock is not a closed one). Rule learned: if a signature is visible enough to be noticed as a choice, it is too visible for a functional icon. Full reasoning in `design/icons.md` |
| | Built the 20-icon set with the signature applied; three flagged as needing optical refinement by hand | Drawing the full set exposed that rule 2 was unenforceable on a third of it — open-form icons have no contour to open. The rule now carries an explicit exception clause |
| | Added a two-rule icon signature — corner radius 2 derived from the UI radius ratio, plus one open aperture at a consistent edge | Distinguishes the set without changing any metaphor. Two levers exactly: one is too subtle, three becomes a different set and costs recognition |
| | Decided against a bespoke icon set: open-source stroke set at 1.5px, with only the product mark and three domain icons drawn by hand | Icons in a support tool are read, not admired — novelty costs recognition. Consistency of weight and grid is what reads as "ours". A full set is days against a one-day timebox and earns nothing in the rubric. See `design/icons.md` |
| | Shell preview caught the same dark-mode control defect one screen later, plus a missing `dir="auto"` on table cells | The rule had already been written down and was still missed. Both are now specified as lint rules in CI rather than as prose — a written rule is not a control |
| | Login preview caught a dark-mode rendering defect: native form controls inheriting the host's appearance, producing black inputs and an invisible button | Added rules 16 and 17 to `design/DESIGN-BRIEF.md` — `color-scheme: light` on the root, and every native control's colours set explicitly. First concrete return on the Phase 3b preview gate |
| | Consolidated `design/tokens.css` from all sources: full palette with four complete state pairs, typography, 8pt grid, shell geometry, Button API, and every primitive's geometry | Token extraction closed. What remains are decisions to confirm (Q-15), not measurements to take |
| | Content-area export added an accent purple family, a universal 1.5px icon stroke, and a 4.5px field radius that contradicts the 8px measured from screenshots | The radius disagreement is left visible rather than averaged: `--field-radius` carries the measured value, `--radius-md` keeps 8px, and one more inspected component settles it |
| | Corrected the shell geometry from layer inspect: sidebar 288px not 226 or 320, header 68px not 60 | Both wrong numbers came from measuring a rendering rather than reading a layer — the exact failure mode `design/figma-workflow.md` warns about, caught here on our own work |
| | Typography, spacing, and colour token names resolved from Figma layer inspect | IBM Plex Sans, 400/700, 8pt grid, `Text/Primary` and `Neutral/800` matching the extracted values. Corrects the earlier reading that colour was not systematised — it is, in Variables rather than Styles |
| | Recorded that 100% line height plus cap-height trim clips Arabic, and decided per-locale leading rather than inheriting it | The source has no answer to inherit, and the defect presents as a font rendering fault rather than a missing token, so it would survive review. See `design/tokens.css` note 2 |
| | Type scale captured from the Figma Text styles panel; family, Arabic face, and spacing scale still open | Sizes and weights are exact. Two findings came with it: every style uses `Auto` line height, which is wrong for Arabic, and colour is not in Styles in any usable form. Both recorded in `11-open-questions.md` Q-13 |
| | Figma extraction blocked — seat rate limit, plus the connector requiring an active canvas selection | Typography and spacing stay as labelled placeholders. A prioritised call order is in `design/figma-extraction-plan.md` so a limited budget is not spent on low-value nodes |
| | Added a preview-before-build gate as Phase 3b of the execution workflow | A rendered preview costs minutes; changing a screen that already has tests, translation keys, and query wiring costs hours. See `design/preview-first-workflow.md` |
| | Resolved the Figma-versus-shipped-app conflict in favour of the shipped app for geometry and layout, keeping the export for exact colour values | The shipped product is what people recognise, and the Figma file is marked "To be completed". Every token in `design/tokens.css` is labelled with its source |
| | Extracted colours, radii, and control heights from the All Requests export into `design/tokens.css`; typography left as a labelled placeholder | Text in the export is outlined to paths, so no font information survived. A plausible guess would look deliberate and be wrong — see `11-open-questions.md` Q-13 |
| | Adopted the existing design system at token and primitive level only, timeboxed to one day; screens are not copied | Tokens carry no domain assumptions and most of the visual resemblance. Copying screens from a different domain imports assumptions this CRM does not share. See `decisions/ADR-009-design-system-source.md` |
| | Added an `AuditLog` table distinct from `TicketHistory`; write path in the skeleton, read endpoint deferred to US-015 | `TicketHistory` cascades with its ticket and covers no auth events, so it is a product feature rather than an audit trail. See `decisions/ADR-008-audit-log.md` |
| 2026-08-23 | Confirmed SQL Server; superseded ADR-001 with ADR-013 and amended ADR-006 | The product owner specified it and the house platform runs it. ADR-001 had itself said to revisit rather than defend if the reviewer expected SQL Server. Four provider-coupled points pinned: `rowversion`, filtered unique indexes, an explicit CI collation on `Email`, and `nvarchar` for anything a human writes |
| 2026-08-23 | Migrated the twelve story-artifact sets into numbered spec-kit features under `specs/`, adding the contract, the frontend guide, the data model, and an `Agent` + `Skill` column on every task | The artifacts predated three decisions and carried no audit obligation, which the NFR-10 architecture test would have failed the build over. The originals are kept as the pre-migration record — thirty of the hundred points are for planning done before an editor is opened, and deleting them would delete the evidence |
| 2026-08-24 | **Rejected ADR-010.** Vertical slices evaluated and turned down; four-project Clean Architecture stands (ADR-002) | The house convention is four-project Clean; the assessment rewards separation of concerns that is *visible* without explanation; and the developer builds fastest in a familiar structure under a deadline. ADR-010 anticipated this outcome and set the rule for resolving it — diverge only if the trade can be stated in one sentence and meant. It could be stated; it could not be meant against those three together |
| 2026-08-24 | Adopted two things from the rejected proposal: feature folders inside `Wasl.Application`, and no repository — `IApplicationDbContext` instead | Rejecting a layout is not rejecting what it was right about. Feature folders give a story diff that lands in one folder while the layering stays visible at the project boundary; `IApplicationDbContext` keeps EF Core out of the Application layer without re-implementing what `DbSet<T>` already is. Two named query classes are the stated exception, and a third needs a written reason |
| 2026-08-24 | Standardised every project name on `Wasl.*` | Two names for one system — 45 files carried `CRM.<project>` across 214 occurrences. The product is named Wasl (`design/brand.md`) |
| 2026-08-24 | **The product scope document arrived after planning was complete.** Mapped all twelve sections in `15-scope-coverage.md`: five covered, seven cut | The document lists roughly sixty features and specifies no deliverables, no acceptance criteria, no technical constraints, and no evaluation criteria — so it is a requirements source, not a backlog. A gap that large between scope and budget means scope selection is part of what is assessed, and every cut carries an argument that would hold with four times the time |
| 2026-08-24 | **Reversed the promotion of US-012** (the provider abstraction, feature `021`) back out of scope | It was promoted on the argument that Communication Channels is a named module and a module resolving to one enum column reads as missing. That argument is reasonable and does not survive nine hours: the seam plus its mock is about an hour, which is a third of a session. The channel stays modelled as domain data, which is what FR-3 asks for |
| 2026-08-24 | **The nine-hour constraint cut Release 1 further**: committed scope is US-005, US-007, US-008, and US-001 seeded rather than built through the UI | Q-5 resolved to three days and about nine hours. That question had predicted this exact set under four hours, and the prediction held. Cut to fit: localization, the audit log, escalation, filters, the timeline, the dashboard, the login screen, and the create-customer UI — each pointing at where its design lives, so these are deferrals rather than decisions |
| 2026-08-24 | Split authentication rather than cutting it: the token endpoint, seeded users, and policies are in; the login screen is out | BR-2 and BR-6 have `403` acceptance criteria, and ADR-005 rejected a forgeable header because it would make every authorisation test pass while proving nothing. The cheap half is what makes those criteria verifiable; the expensive half is a screen |
| 2026-08-24 | Recorded a conflict rather than resolving it quietly: the nine-hour plan cuts localization and the audit behaviour, and `08-board.md` says neither is ever cut | Both positions are defensible and were written under different budget assumptions — at 20–25 hours localization is roughly a tenth, at nine it is roughly a third. ADR-007 and ADR-008 argue about *cost*, not correctness, so the retrofit is expensive rather than impossible. Needs the product owner: if bilingual is non-negotiable, US-007 leaves the committed scope |
| 2026-08-24 | **Localization and audit reduced to their minimum viable form** under the nine-hour constraint. The infrastructure exists and is proven; the language switcher screen (US-014) and the audit read endpoint (US-015) are deferred | The `08-board.md` "never cut" line was written under a 20–25 hour assumption and is **superseded by this row, not contradicted by it**. Cutting them outright would lose two things the product scope document explicitly asks for — section 10 (audit logs) and section 12 (Arabic and English). Reduced form: no hard-coded string from the first line of code, a catalogue with a key for every string, ~30 keys translated to Arabic, RTL verified on the two screens that exist, plus `dbo.AuditLog` and the `AuditBehaviour` writing in the same transaction. ~1h45 rather than ~3h, so **US-007 stays in scope**. The expensive half of each was never the capability — it was the retrofit, and the retrofit is only expensive if the discipline is skipped at the start |
| 2026-08-25 | **001-solution-skeleton delivered.** Four projects, three test projects, `IApplicationDbContext`, the UTC converter, `Customers` + `InitialCreate`, `GET /health`, and CI green on `ubuntu-latest` — 17 tests, 0 warnings | Six defects were caught by the gates rather than by review, and not one by reading code: a high-severity advisory in a transitive package (build gate), `global.json` resolving to the installed preview SDK (AC-13), an architecture test that was a **false negative** until someone tried to break it, two `/health` contract violations against the frozen contract, an integration suite that had never talked to its own container (CI, not local), and a CI guard that parsed human-readable output and broke when the format differed. Full evidence in `specs/001-solution-skeleton/tests.md` |
| 2026-08-25 | **002-error-contract core delivered.** Domain exception hierarchy, the 13-row `ProblemTypes` registry, one `ProblemDetailsFactory`, one `traceId` accessor, `ValidationBehaviour`, and 16 new tests — 33 tests, 0 warnings. `002b` deferred with a per-task reason | The contract did not move: all 13 registry rows are in the core and only the code paths that *raise* some of them are deferred, so a client written against `contracts/error-contract.md` today is written against the final shape. Four defects found by running rather than reading, the important one being a **captive dependency** — `AddExceptionHandler<T>` registers a singleton and the factory was scoped, which .NET only validates in Development, so the test environment started cleanly and Development refused to build. It was caught by AC-13, a criterion written for an entirely different reason. One real contract deviation is recorded rather than smoothed over: `errors` keys come back PascalCase where the contract's field names are camelCase, fixed in `002b`. Full evidence in `specs/002-error-contract/tests.md` |
| 2026-08-25 | **003-audit-trail core delivered.** `dbo.AuditLog`, the diff interceptor, redaction, `TransactionBehaviour` + `AuditBehaviour` in `Wasl.Infrastructure`, and one ordered behaviour registration in `Wasl.Api` — 93 tests, 0 warnings, 60 of them new. `003b` (the `wasl_app` role, `DENY`, the restricted connection, AC-12/AC-13) deferred **whole** | Three things stopped being remembered discipline: writing the row, opening the transaction, and declaring an audit action — the last one a build failure. Two findings are worth the record. **A missing index that the acceptance criterion would have passed:** two indexes over `OccurredAtUtc` written with the unnamed `HasIndex` overload silently merged, the migration came out with three, and the one that vanished was the *unfiltered* one — so a check asserting "the filter is present" was satisfied. Found by reading generated code, which no test could have caught because the test shared the wrong assumption. **And the diff was watched failing:** moving the interceptor to `SavedChanges` turned four tests red while the row stayed, `COUNT(*)` stayed 1, and `Changes` came back null on every command — so asserting presence rather than content would have shipped a trail that records that something happened and never what. Deferring least privilege whole rather than halved is stated outright: append-only is an application property until `003b`. Full evidence in `specs/003-audit-trail/tests.md` |
| 2026-08-25 | **Four scope decisions on opening `009-create-ticket`**, and one of them was forced by a specification that described a database which did not exist | **`009` ships without authentication**: `004` comes after it and after `012` in the plan, so `createdByUserId` is null in the `201` — the field stays in the response and nullable in the DTO, because removing and re-adding it is a breaking change for the frontend while a null is not. The frozen contract does not change; AC-12/AC-13 are deferred to `004` by name. A stub claim was rejected on ADR-005's own argument. **The BR-1 map and all 36 transition tests moved from `012` to `009`**: AC-10 returns `allowedTransitions`, and shipping the map with 35 unverified cells would put an untested rule behind an API the screen renders buttons from — the failure class `003` caught twice. `012` keeps `PUT /status` and optimistic concurrency. **`GET /api/tickets/{id}` moved from `010` to `009`**: the frozen contract promises `Location` resolves, and `010` lands after `012`, so the alternative was a `201` whose `Location` returned `404` for the whole demo. `010` keeps the list and both screens. **The create-ticket form is `024-frontend-create-ticket-form`**, not `023` — `023` is the frontend foundation and would otherwise grow with every screen and lose its definition of done. `16-three-day-plan.md` amended: Session 2 item 1 struck through and folded into Session 1 item 6, budget 50m → ~2h, stated rather than absorbed |
| 2026-08-25 | **`009`'s `data-model.md` described a database that did not exist.** Three statements were wrong, and four foreign keys stood on them | It claimed `dbo.SupportUsers` was created by `001` — `001` created `Customers` alone, and `SupportUsers` appears nowhere in source; that `dbo.AuditLog` was created by `001` — `003` created it; and that `008` had added `IX_Customers_FullName` — `008` is not built. `CreatedByUserId` was additionally `NOT NULL` with a key into the missing table, which collided with the no-authentication decision on the same column. Resolved: `CreatedByUserId`, `PerformedByUserId`, `AssignedToUserId` and `EscalatedByUserId` are `uniqueidentifier NULL` with **no** foreign key in `009`; `004` creates `SupportUsers` and adds all four keys in the migration that creates the table. Seeding a "system" user to satisfy the key was rejected — ADR-005's objection exactly, and every audit row would then name a user the server never authenticated. The correction is a table at the top of `data-model.md` rather than a silent edit, because a specification describing state that does not exist makes every decision after it stand on invented ground, which is what happened here |
| 2026-08-25 | **`CommunicationChannel` was implemented with the wrong members and corrected before it reached a migration.** `Email · Phone · WhatsApp · Portal` → **`Email · WhatsApp · LiveChat · Sms · WebForm`** | Caught by the product owner against the product scope document, section 3. **No specification was wrong** — the blueprint states the five values correctly in six places: FR-3.1 in `01-product-spec.md`, twice in `03-domain-model.md` (the column comment at line 41 and the enum at line 372), `00-project-context.md`, `15-scope-coverage.md` row 3, and `design/icons.md`. The error was mine: the enum was written from the `009` contract's example payload, which shows only `"channel": "WhatsApp"`, and the remaining members were invented rather than read from `03-domain-model.md`. `Portal` was the worst of them — `15-scope-coverage.md` excludes a customer portal outright, so the enum asserted a capability the scope document had already ruled out. Consequences checked rather than assumed: no `.cs` file anywhere referenced the removed members; the values are persisted as strings (ADR-013) and no migration had been generated yet, so there was no data to migrate; and the i18n keys in `009/frontend-spec.md` already carried the **correct** names, with the key holding the wire value (`tickets:channel.Sms`) — so the enum was the thing out of step with them, not the reverse. The member names are now documented in the type itself as a contract: `design/icons/` keys one asset per channel by name, and the frontend lane builds its icon map from the enum as it appears in the frozen contract, so a rename presents as a missing asset rather than as a type mismatch |
| 2026-08-26 | **Each layer now registers itself.** `AddApplication()` · `AddInfrastructure(config)` · `AddPresentation()`, composed in three chained calls; `Program.cs` names no type from another layer | `Program.cs` had been registering `HttpCurrentUser`, `HttpRequestContext`, `StaticProblemMessageSource`, `ProblemDetailsFactory`, `TimeProvider` and a `WaslDbContext` health check by name. Inside one assembly that works — and it is the habit that, one layer over, forces a type public purely so the composition root can register it. The difference between a layer that is isolated and a layer that is *called* isolated is whether anything outside it can name its parts. `TimeProvider` moved to `AddInfrastructure` (the layer that reads it) and the `AddDbContextCheck<WaslDbContext>` moved with the `HealthChecks.EntityFrameworkCore` package, since Infrastructure owns the context — that was the one Infrastructure type still leaking upward. **One instruction could not be followed and the reason is a prior decision:** the request was for `AddApplication()` to register the pipeline behaviours in order. `TransactionBehaviour` and `AuditBehaviour` live in `Wasl.Infrastructure` (003 R-14) and `Wasl.Application` sits below it, so that does not compile — and per-layer registration is exactly what 003 R-15 observed inverting the order to `Transaction → Audit → Validation`. `AddWaslPipeline()` stays in `Wasl.Api`, called last, as the documented exception; the comment at both the call site and the extension says the order is deliberate rather than alphabetical |
| 2026-08-26 | **The integration suite was running seven SQL Server containers at once and died of it.** Switched from `IClassFixture` to one `ICollectionFixture` shared by every class | `xUnit` creates an `IClassFixture` **per test class**, so seven classes meant seven containers starting concurrently at roughly 2 GB each. The suite failed with `System.OutOfMemoryException`, and the failures landed on unrelated validation assertions — so it read as a `009` bug rather than as resource exhaustion. **It was invisible until the whole suite ran together:** every class passed under `--filter`, because one class is one container, which is the lesson worth keeping — a green filtered run is not evidence about the suite. Wall clock for the integration project fell from 1m29s to 49s. The cost is accepted and recorded in the collection's own remarks: a collection runs its classes sequentially, and its tests share one database, so every assertion scopes itself by id or by audit action rather than counting rows in a table |
| 2026-08-26 | **`009-create-ticket` backend delivered.** `Ticket` + `TicketHistory` + `dbo.TicketNumberSeq`, `POST /api/tickets`, `GET /api/tickets/{id}`, the BR-1 map with **all 36 cells in both assignee states**, `IAuditableEntity` stamping in `SaveChangesAsync`, and per-layer DI — 214 tests, 0 warnings, 121 of them new | `CreateTicketCommand` is the first production `IAuditableCommand`, so `003`'s NFR-10 scanner now runs over a real population and a later command without an audit action fails the build. **Three defects would have shipped.** Enums bound from **numbers**: every request in the first integration run returned `400`, including one that should have been `404`, because `System.Text.Json` binds enums from integers by default — and a bound request would have serialised `status` as `0`, leaving a client branching on integers whose meaning changes when someone reorders an enum. The contract had shown `"channel": "WhatsApp"` since it was frozen; `002` never hit it because `002` has no enum on the wire. **A column `DEFAULT 'Normal'` on `Priority`** would have stored an explicit `Low` as `Normal` — EF warned that a database default applies whenever a property holds the CLR default, and `Low` is that default. AC-8's default belongs in one place and the handler already had it. **Seven SQL Server containers**: `IClassFixture` is per class, so the suite died of `OutOfMemoryException` with the failures landing on unrelated validation assertions — and it was invisible under `--filter`, because one class is one container. Fixed with one `ICollectionFixture`; the integration project went from 1m29s to 27s, and `CLAUDE.md` gained the rule that verification means the whole suite. **And one that was mine:** `CommunicationChannel` was written with two invented members, one of them (`Portal`) contradicting a scope document — the five correct values were stated in six places in the blueprint, including the enum itself in `03-domain-model.md`. Caught by the product owner, not by a test, because a wrong enum compiles. Full evidence in `specs/009-create-ticket/tests.md` |
| 2026-08-26 | **`012-change-ticket-status` backend delivered.** `PUT /api/tickets/{id}/status`, BR-1 enforced in the entity, three distinct `409` codes, explicit optimistic concurrency, and both ordering decisions asserted — 250 tests, 0 warnings, 36 new | Smaller than its estimate because `009` had already built the BR-1 map and its 72 cell assertions; what remained was the endpoint, the version check, and the **order** — which turned out to hold the real risk. Two orderings are asserted at the endpoint and again in the domain: `Closed → Closed` reports `ticket-closed` rather than `same-status-transition`, because a closed ticket does not become un-closed by reloading; and a stale `expectedVersion` beats a forbidden transition, which the contract calls the easiest to get wrong — judge the transition first and every stale UI reports a rule violation that does not exist, naming a `currentStatus` the user cannot reconcile with their screen. The version check is therefore an **explicit** comparison rather than a caught `DbUpdateConcurrencyException`: that exception only surfaces after the write, which would put the check in exactly the wrong place. **Three `409` codes where one would have compiled** (`spec.md` Q-3): the client refetches quietly on same-status, offers Assign on assignee-required, and offers a different transition on a forbidden cell — distinctions it cannot make by parsing English. **One defect found by an acceptance criterion doing its job:** AC-3 requires the `409` detail to *name* the current status and the permitted list, and the detail came back as the raw message key — `002`'s message source had no entry, and its deliberate key-instead-of-throw fallback made the response well-formed and useless. A test asserting only that `detail` was present would have passed. Also recorded: `docs/sdd/04-business-rules.md` line 17 still shows `PendingCustomer → PendingCustomer` as permitted where every other diagonal is `–`; `CLAUDE.md`, BR-1.9 and shipped code all say forbidden, so the blueprint is the one file out of step and the correction is a proposed amendment rather than an edit from inside a feature. Full evidence in `specs/012-change-ticket-status/tests.md` |
| 2026-08-26 | **BR-1's `PendingCustomer` diagonal corrected in `docs/sdd/04-business-rules.md`.** `✅` → `–`, closing `DOC-012-02`'s first half | The one cell where the blueprint disagreed with everything else: `CLAUDE.md`'s copy of the table showed `–`, BR-1.9 states that a same-status transition returns `409`, and `009` shipped it as forbidden with a test covering all six statuses. Five of the six rows already carried `–` on their own diagonal. Corrected **in the blueprint** rather than left as a proposal, on product-owner instruction and for the reason they gave: it is not a design change, it is one file contradicting the code, `CLAUDE.md` and its own rule at once — and leaving it means the next person to read it builds on a rule that does not exist. `012`'s `spec.md` Q-4 predicted the ruling was needed; the ruling happened when `009` was reviewed. The second half of `DOC-012-02` — adding `errors/same-status-transition` and `errors/assignee-required` to `05-api-conventions.md`'s `409` inventory — is still open |
| 2026-08-26 | **`010-ticket-list-and-detail` backend delivered.** `GET /api/tickets` — paged envelope, BR-7.2 clamping, newest-first, customer name projected in the same query — 263 tests, 0 warnings, 13 new. Scope trimmed on instruction: filters, search and column sorting to `015`; both screens to the frontend lane | **Two enum values from `009` were wrong and are corrected here:** `TicketPriority.Urgent` → `Critical` and `TicketCategory.Complaint` → `Account`, with both declaration orders aligned to `docs/sdd/03-domain-model.md` lines 368 and 370. `010`'s frozen contract states the same values. **Same mistake as `CommunicationChannel`, same cause** — the enums were written from a contract example rather than from the line that defines them — and no test caught it because `009`'s tests used the two values that happened to be right. A test asserting a wrong value against a wrong enum passes. `010`'s row test now asserts every enum value **by name against the contract**. No data migration: the values are stored as strings and no row held either name. **And one guard is recorded as unproven rather than claimed:** AC-22 wants a stable sort, so the query orders by `CreatedAtUtc` then `Id` — but no test in the suite fails without the tie-break. The first version of that test created six tickets through six HTTP requests, which are six scopes and therefore six distinct instants, so the tie it existed to test never arose and it passed with the tie-break removed. Rewritten to force the tie, it still passed: for six rows SQL Server returns them in clustered-key order. The tie-break stays because the engine promises no order for a tie, and `tests.md` says outright that nothing demonstrates it — proving it needs a volume and plan shape the test strategy excludes. A comment claiming a test protects something it does not is worse than no comment |
| 2026-08-26 | **Demo readiness: a one-command seed, a README with what is NOT built, a clean-clone run, and a timed rehearsal.** The rehearsal found a live contract violation on the demo path | `dotnet run --project src/Wasl.Api -- --seed` applies every migration and writes three customers and five tickets in five statuses **through the real domain** — `Ticket.Create`, the BR-1 machine, the sequence, the history rows — so a status the state machine forbids fails the seed rather than producing a ticket the product could not. Idempotent, because a demo rehearsed three times must not end with fifteen tickets. Verified from a **fresh clone into a sibling directory against an empty database**: three migrations applied, five tickets written, `/health` healthy, the list correct, 166 domain tests green, build clean with warnings as errors. The clone went to a sibling path rather than deleting the working folder, which holds the frontend lane's uncommitted work. **And the rehearsal earned its place:** a malformed body came back with `"type": "https://tools.ietf.org/html/rfc9110#section-15.5.1"` instead of `errors/validation`. `[ApiController]`'s automatic model-state filter short-circuits **before** any handler and before `UseExceptionHandler`, so nothing `002` built ever saw the request — an unparseable enum, a malformed `Guid` in the body, or a truncated payload all bypassed the one producer AC-2 promises. `002`'s own test for AC-2 grepped `src/` for constructors of `ProblemDetails`, which was true and insufficient: the framework constructs its own inside itself, where no grep reaches. Fixed with an `InvalidModelStateResponseFactory` routed through the existing factory, registered once in `AddPresentation` rather than per controller, and covered by four new tests — including one asserting that a binding failure and a FluentValidation failure are **indistinguishable to a client**, which is what AC-2 was actually for. `002` A-2 had assumed a malformed body would surface as a `BadHttpRequestException`; the observed behaviour is more mundane — it never becomes an exception at all |
| 2026-08-27 | **Sideways review of `002`, `003`, `009`, `010`, `012` against `CLAUDE.md`'s twelve-row concurrency-and-abuse checklist: one defect fixed, four gaps recorded, eight items accepted with reasons** | **Fixed — `Customers.IsActive` carried `DEFAULT 1` while the CLR default for `bool` is `false`**, so any code deactivating a customer would have stored them as active with no error. Identical in shape to the `Priority` defect `009` found, and sitting in `001`'s configuration since day one — unreachable only because `Customer` has no factory until `007`, which is also the feature where deactivation starts to matter. It needed a migration, not one line: dropping a column default is a schema change. **Fixed — the seeder was writing state outside the pipeline**, so the demo would have opened on an empty `dbo.AuditLog` after twelve state changes, with BR-9 being one of the strongest claims this codebase makes. Rerouted through `ISender`: it now writes **14 audit rows** and, more usefully, walks the path a user walks — so anything that breaks the pipeline breaks the seed before a demo rather than during one. **That change immediately caught a bug in the seeder itself:** the assignment shortcut issues a raw `UPDATE`, which moves the `rowversion`, so the token from the create response was stale and the pipeline refused it with `ConcurrencyConflictException` — the seeder behaving exactly like the second browser tab AC-17 exists to reject. The direct-context version could never have surfaced that. Recorded, not fixed: `POST /api/tickets` is not idempotent (no AC asks for it, but the question is now asked and answered in the README); the `404` for an unknown `customerId` becomes an enumeration oracle when `004` lands, with both available shapes written into `009`'s `tests.md`; `expectedVersion` validation allocates a buffer the size of the input, where a Kestrel body limit is the cleaner fix and `004` owns hardening; and `MultipleActiveResultSets=True`, inherited from `001` and used by nothing, disables EF savepoints — not a correctness defect since `TransactionBehaviour` rolls the whole transaction back, but it logs on every save and a warning that always fires is a warning nobody reads. Also fixed in passing: `010`'s tie-break test had been asserting "the newest six" with a **past** timestamp, so other test classes' newer tickets filled both pages — the shared-database constraint `CLAUDE.md` records, biting the test that was written to respect it |
| 2026-08-27 | **`004-auth-and-roles` backend half delivered.** `dbo.SupportUsers` + the four FKs `009` deferred, two seeded users, `POST /api/auth/token`, HS256 with a role claim, real `ICurrentUser`, `ManagerOnly`, and `RequireAuthenticatedUser` as the **fallback** policy — 303 tests, 0 warnings, 36 new. The frontend half (AC-24…AC-30) is the frontend lane's | **`004` does not implement BR-2 — it builds the identity BR-2 stands on**, and that distinction is what kept a fake actor out: no seeded "system" user, no header, no constant claim, all three rejected by ADR-005 by name. The payoff arrived without a line of application code: `Ticket` is an `IAuditableEntity` and the stamping lives in `WaslDbContext.SaveChangesAsync`, so the moment a token started arriving, `CreatedByUserId` and the three actor columns on `dbo.AuditLog` filled themselves. Tests that asserted `null` were updated to assert the real id — a value change, not a contract change, because `009` kept the field in the response shape rather than adding it later. **The negative control found something the plan did not.** Reverting `MapInboundClaims` and `ClockSkew` to their defaults turned four tests red; three were predicted by `spec.md`'s own table, and the fourth was that the audit trail's actor columns silently went null — every request still succeeding, nothing thrown, and the strongest claim this codebase makes quietly untrue. The chain runs `ICurrentUser` → `IAuditableEntity` stamping → `AuditBehaviour`, three features away from the setting. The same revert also `403`s **every** Manager, which is why AC-7 asserts the Agent's refusal too: a mismatch makes both fail, so asserting one side alone would have looked identical to asserting the other. **Three defects found by running, each of them `004` breaking something older.** `JwtSecurityToken` emits `nbf` and `exp` from its arguments but **not** `iat` — found by decoding a token from the live app while writing the README's curl example, and AC-2 would otherwise have passed against a token no client could read an issue time from. `DemoSeeder.AssignAsync` and `ChangeTicketStatusTests.AssignAsync` both wrote a fabricated assignee `Guid`; `009` documented that as safe because no foreign key existed, and `FK_Tickets_Assignee` killed the seed on `Error Number:547` and every `InProgress` test with it. Worth stating precisely: the fabricated id was **never** valid — it was an unenforced dangling reference for two features, and `004` did not create the defect, it made the defect fail. **And one assumption written as a coherent comment before being tested:** the failure path was expected to write `Auth.LoginFailed`, but `AuditBehaviour` composes every row with one `AuditAction` string and no knowledge of which path ran, so a failed sign-in wrote `Auth.LoginSucceeded / Failed` — a row contradicting itself. Resolved as one outcome-neutral action, `Auth.SignIn`, a deviation from BR-9's naming table recorded with its reason: splitting it puts the distinction in two columns that must agree and eventually would not. Six deviations in all, in `specs/004-auth-and-roles/tests.md`. **Three things are named as open rather than done:** AC-17/AC-18 — no audit row on a `401` or a `403` — is **a gap in BR-9.4**, needing an `IAuthorizationMiddlewareResultHandler`, deferred to `004b`; there is no rate limit or lockout on `POST /api/auth/token`, so brute force is unimpeded and one identical `401` per wrong input does nothing to slow a script; and AC-15's literal wording is unmet, because a request to the token endpoint is anonymous **by definition** and `ICurrentUser` has nothing to snapshot — the user is on the row in `EntityId` and `EntityLabel`, and the test asserts the null explicitly so the limit is visible in the suite rather than absent from it. Full evidence in `specs/004-auth-and-roles/tests.md` |
| 2026-08-27 | **The development connection string pointed at a database that only existed on one machine.** `Server=.\SQLEXPRESS` → the `docker-compose.yml` container on port 14330. Supersedes `001` AC-10 | `quickstart.md` and the README both say `docker compose up -d db`, and `docker-compose.yml`'s own comment said the application does not need it — so a clean clone that followed the instructions started a container the application never spoke to. **`/health` was not lying:** `AddDbContextCheck<WaslDbContext>` is registered, so it reported `database: Unhealthy` correctly — and being correct is what made it read like a broken health check rather than a wrong address. Reported by the frontend lane from a real run. `001` AC-10 required Windows auth and no password in any file; the `sa` password is the same throwaway already committed in `docker-compose.yml`, so honouring the criterion was protecting nothing while making the documented path fail on any machine without a local named instance. Verified end to end afterwards: `docker compose up -d db`, `dotnet ef database drop -f`, `dotnet run -- --seed` writing 2 users + 3 customers + 5 tickets + 14 audit rows, then a second `--seed` writing nothing, then `/health` healthy with `Content-Language: en`, `GET /api/tickets` returning `401` without a token, `MANAGER@WASL.LOCAL` signing in against a stored `manager@wasl.local`, and the paged list answering with the bearer token |
| 2026-08-27 | **`errors` keys were PascalCase where the frozen contract says camelCase — and two passing server tests were agreeing with the defect** | FluentValidation reports the CLR property name and `ProblemDetailsFactory` passed it through, so every client lookup by the contract's key found nothing and a `400` rendered as a validation error pointing at no field. Fixed with one `CamelCase(field)` helper applied in both `FromDomainException` and `FromValidationFailures`. **The record is about how it survived:** `002`'s `ErrorEnvelopeTests` asserted `errors.TryGetProperty("FullName")` — written from the implementation rather than from `contracts/` — so the suite confirmed the wrong casing twice. Found by the frontend lane running the real API. The lesson is narrower than "test more": a test written by reading the code it tests can only confirm that the code does what it does. Recorded under *Contract changes* in `specs/009-create-ticket/plan.md` |
| 2026-08-27 | **Process: gate 3–4 of `CLAUDE.md` was skipped.** `004` was opened and implemented on a locked scope without returning for the explicit approval the working agreement requires | Caught mid-flight by the product owner. The scope was then written out in full, reviewed and approved, and the work continued rather than being discarded — `004` was the right feature and the ruling said so. Standing consequence, recorded because a process failure that leaves no trace repeats: opening another feature this way stops all work for a review from the beginning. The gate exists because it is the only thing that tells the product owner what is being built while they cannot see the code |
| 2026-08-27 | **The schedule constraint was lifted: three additional days granted.** `docs/sdd/PHASES.md` is the governing plan from this date. `16-three-day-plan.md` marked **superseded** and kept; `08-board.md`'s three-day section removed; `START-HERE.md` no longer routes readers to the nine-hour plan | The nine-hour budget answered `11-open-questions.md` Q-5 and drove a narrowed commitment — `US-005` · `US-007` · `US-008` plus `US-001` seeded rather than built — and drove localization and audit down to their minimum useful form. All of that was argued rather than assumed, so the file is **superseded rather than deleted**: what was cut under a hard constraint, and why, is evidence about how the constraint was handled, and a deleted plan cannot show it. Where the two disagree, `PHASES.md` wins. **Three things this does not change.** Product-level scope selection against the twelve sections of the supplied document stays in `15-scope-coverage.md` — those seven cuts are **decisions** whose reasons hold at four times the budget, and a longer clock is not an argument against any of them. The reduced forms already delivered stay delivered: `dbo.AuditLog` and the audit behaviour exist, every server-authored message is a symbolic key rather than a sentence, and the deferred halves — `005`'s catalogues, `019`'s read endpoint — become buildable rather than cut. And the working agreement's gates are unchanged: more time is not permission to skip a spec, which is worth stating on the same day `004` was recorded as having skipped one. `08-board.md`'s "never cut" paragraph is the standing position again, with the nine-hour note kept beneath it as the record of what shipped under the constraint |
| 2026-08-28 | **`011-assign-ticket` backend delivered.** `PUT /api/tickets/{id}/assignee` (assign · reassign · unassign), `GET /api/support-users`, `Ticket.Assign`, two new history event types, four new exception types, two new registry rows, and a second seeded Agent — 340 tests, 0 warnings, 37 new. No migration. The picker UI is the frontend lane's | **The whole feature turns on one measurable claim: a handler denial is audited, a policy denial is not.** `CLAUDE.md` mandates BR-6's split — role-only checks as endpoint policies, data-dependent checks in the handler — and `011` is where that stopped being architectural taste. `003`'s `AuditOutcomeClassifier` maps a `DomainException` carrying `DomainErrorCodes.Forbidden` to `Denied` and `AuditBehaviour` writes it through `WriteIndependentAsync`, on its own connection, so it survives the rollback of the write it refused. A `403` from the authorization middleware throws nothing, MediatR never sees it, and no row is written — `004` AC-18, still open. **Measured rather than argued:** moving the check into `[Authorize(Policy = ManagerOnly)]` and re-running returned `found 0: {empty}` — the row does not lose a column, it ceases to exist, while the API still answers a perfectly correct `403`. AC-17 was added to the spec by the reconciliation so the property is asserted rather than believed. **The same control found two things nobody predicted:** a policy's `403` has an **empty body** — no `type`, no `traceId`, nothing a client can branch on, because `002b` still owns enveloping middleware-produced statuses; and a policy **cannot express the contract's check ordering at all**, since authorization necessarily runs before any handler, so `409`-before-`403` becomes unreachable by construction. Both reached the spec's conclusion by routes the spec did not draw. **A defect two releases old, exposed here:** `TicketHistory.PerformedByUserId` was NULL on every row this system had ever written. The stamp in `WaslDbContext.SaveChangesAsync` matches `IAuditableEntity`, and `TicketHistoryEntry` is correctly not one — it is append-only and its actor column means "who did this" rather than "who last edited this row" — so the loop skipped it, and `Created` rows from `009` and `StatusChanged` rows from `012` all carried a null actor. Nothing failed; `013`'s timeline would have said "someone" for every event. Found because AC-9 asserts the actor rather than the row's existence, which is `CLAUDE.md`'s "assert content, not presence" catching its third defect. Fixed in one place beside the existing stamp, keeping the standing rule that the time and the user are set in `SaveChangesAsync` and never in a handler. **Q-5 settled by addition:** the frozen contract returns `assignee` as a nested object while `009` and `010` froze a bare `assignedToUserId` — three shapes for one concept. Aligning all three is a breaking change to two contracts `024` has already built against, so `assignee` was **added** to the one shared DTO and `assignedToUserId` kept; adding a field is backward-compatible and a second seventeen-field DTO with its own mapper is the "second shape to keep in step" `012` declined. The redundancy and the paged list's third shape are recorded as `010`'s to resolve, breaking, when it happens. **And the build lied.** Reverting a negative control with `Copy-Item` restored the source with an **older** `LastWriteTime` than the compiled DLL, so MSBuild skipped recompiling and reported `0 Errors` — the second control was measured against the first one's binary and would have been written up as "swapping the check order breaks five tests", specific and confident and wrong. The only tell was that the failures did not match the change: swapping two checks cannot make a `403` arrive with an empty body. Fifth entry for the list of tools that produce well-formed reports about nothing, and the first where the tool was the build. Every control re-measured with `--no-incremental`. Full evidence in `specs/011-assign-ticket/tests.md` |
| 2026-08-28 | **`011`'s `data-model.md` described a database that did not exist, and `plan.md`'s central design rested on a rejected ADR.** Both corrected before implementation, in tables rather than silent edits | Four rows of `data-model.md` were wrong: `FK_Tickets_Assignee` and the assignee index were attributed to `009`, which deferred all four FKs by name — `004` created them, and the index is `IX_Tickets_AssignedToUserId`, not `IX_Tickets_Assignee`, so the file's advice "named here so nobody adds a second one" pointed at an index that does not exist; `dbo.SupportUsers` was attributed to `001` with `IsActive bit NOT NULL DEFAULT 1`, where `004` created the table and deliberately gave it **no** column default (EF applies one whenever the property holds the CLR default, and for `bool` that is `false`, so deactivating a user would have stored them active — the identical defect `001` shipped on `Customers.IsActive`); and the file claimed `dbo.AuditLog` carries this feature's `Auth.Forbidden` rows, when **nothing writes that row at all**. The last one had teeth: it is what decides where a BR-2 check lives, and believing it would have made a policy look auditable. Separately, `plan.md` specified a `TicketAssignmentPolicy` class in `Wasl.Domain` on the stated premise that *"ADR-010 removed the `Wasl.Application` test project"* — **ADR-010 was rejected and that project exists**, so the premise is false and the plan's own quoted objection was right: BR-2.1–BR-2.3 need the caller's identity and role, which are not ticket concerns. The rules went into the handler and the two genuine ticket invariants (BR-2.5, AC-11) into `Ticket.Assign`. This is the second feature in a row whose planning artifacts described state that did not exist — `009` was the first — and the pattern is the same: an artifact written before its dependencies shipped, read later as fact |
| 2026-08-28 | **`004b` — the `401` body did not match its own frozen contract, and the guard written to stop that recurring found seventeen more of the same defect.** 355 tests, 0 warnings, 15 new. Reported by the frontend lane from a live run | Two faults in one body: `title` was *"Authentication is required."* where the contract says *"Email or password is incorrect."*, and `detail` was **`Error.Auth.InvalidCredentials`** — a raw resource key rendered verbatim on the login screen, which BR-8.6 forbids. **Two separate causes, neither a contract change.** One `type` legitimately covers two situations with different correct titles — *no credentials supplied*, produced by the middleware, and *credentials rejected*, produced by the handler — and the frozen contract shows both, while the registry has one `TitleKey` per row. Fixed with an optional `TitleKey` on `DomainException`, null by default, preferred by the factory; the `type` deliberately did **not** change, because `type` is what a client branches on and is frozen, so splitting it would break every consumer to solve a wording problem, whereas the title is the translated half nothing should branch on. The `detail` leak is closed by `CarriesDetail: false` on that row, which matches both contract examples and is the stronger answer here anyway: a rejected sign-in must read identically whichever of the three causes it was, and every extra sentence is one that can accidentally distinguish them. **The second occurrence is what changed the response.** `012` AC-3 had already caught one — a `409` whose `detail` came back as `Error.Ticket.InvalidTransition` — so this time the fix included a guard rather than one more catalogue entry. `ResourceKeyLeakTests` asserts that no `title`, `detail`, or `errors` message in any error response matches the **shape** of a resource key (`Word.Word.Word`, no spaces; no English sentence matches). **On its first run it failed on two more cases, and those two turned out to be the entire API:** every FluentValidation message key was unresolved — seventeen of them, so every form field on every screen was rendering `Validation.Ticket.SubjectRequired` and its siblings to the user. **Not one server test had noticed**, because each asserted that `errors.subject` was *present* and held one entry; `CLAUDE.md`'s "assert content, not presence" failing in the one direction the existing assertions were blind to. A build-time half was added beside it: `MessageKeyCoverageTests` scans `Wasl.Application` and `Wasl.Domain` for literals shaped like a message key and asserts each is in the catalogue, with no database, so a key on a rarely-taken branch fails on the commit that introduces it rather than on the request that renders it — and it asserts its own scanner against three keys and three sentences, because `001` shipped an architecture test that was a false negative until someone broke it on purpose. Both guards verified by deleting one entry and watching each go red, then restored: 355/355. The reverse direction is deliberately not asserted — an unused catalogue entry is harmless, and `002` registered titles for statuses it did not yet raise on purpose, so failing on those would punish exactly the discipline that made this a catalogue edit rather than a redesign. **Unchanged and still open:** AC-17/AC-18, the missing audit row on a middleware denial, which is `004b`'s other half and untouched by this. Full evidence in `specs/004-auth-and-roles/tests.md`; the contract reasoning in that feature's `plan.md` under *Post-delivery contract notes* |
| 2026-08-28 | **Two pagination shapes are now deliberate, and `CLAUDE.md` says so under *API contract*** | `010` froze the envelope `{ items, page, pageSize, totalCount, totalPages }`; `013` will use a `before` cursor for the ticket timeline. The distinction is not a preference: a ticket **list** grows at the end the user is not reading, so page 2 stays page 2, while a **timeline** grows at the end they are reading, so a page number silently skips or repeats entries between two requests. `015` takes the envelope. Written down on the product owner's instruction and for their stated reason — without the paragraph, the first person to meet both reads it as an inconsistency and unifies them, and unifying them means choosing one failure mode for both |
| 2026-08-28 | **`013-ticket-timeline-and-comments` backend delivered.** `dbo.TicketComments`, `POST /api/tickets/{id}/comments`, `GET /api/tickets/{id}/timeline` with a cursor, `TicketTimelineQuery` in `Infrastructure/Queries/` — 378 tests, 0 warnings, 23 new, **run twice identically** because a feature about ordering is not proven by a suite that passes once. Timeline UI is the frontend lane's | **This feature could prove two things three older ones could not.** First, AC-18: `003` registered `TicketComment.Body` in `AuditRedaction` before any comment existed, so the rule had **never once fired** — `013` is the first feature that can exercise it, and the assertion searches every column of the audit row for a distinctive string rather than reading the redaction list. It also asserts the row is **not empty**, because a redaction that worked by writing nothing would pass "the secret is absent" and prove nothing; the diff still names the entity and the field with only the value replaced by `[redacted]`. Confirmed against the live database as well as in the suite: `Hits: 0`. Second, the tie-break. `010` shipped the same kind of stable-sort guard and recorded it **unproven** after three attempts, because six HTTP requests are six scopes and six distinct instants. Here a tie is guaranteed by construction — `IRequestTimestamp` memoizes the clock once per request and adding a comment writes two rows in that request, so **every comment produces two timeline entries with a byte-identical timestamp**, confirmed live at `12:19:53.821Z` on both. Deleting the tie-break turns a test red. **And one result from that control is worth more than the pass:** the test asserting *repeatability* did **not** go red — SQL Server returned the same order twice anyway on a small dataset, exactly `010`'s finding. So the test that catches a missing tie-break is the one asserting a **specific order**, and the repeatability test earns its place only by proving a tie exists at all. **A cursor defect nearly shipped.** The first implementation ordered by `Id` and filtered the cursor by comparing the id **as text** — and SQL Server orders `uniqueidentifier` by a byte order of its own, which is not lexical, so an entry sorted *after* the cursor by the `ORDER BY` and *before* it by the `WHERE` and **appeared on two consecutive pages**. Caught only because AC-12's test asserts that no entry appears twice; a test counting four entries per page would have passed while a reader saw one comment twice and never saw another. The rule that came out of it — *the cursor must compare exactly the keys the sort orders by, in the same sequence* — was then broken deliberately in both directions, and both produced a broken feed. Three negative controls in all. **`spec.md` A-4 was also more specific than the code:** ties break by **type then id**, and the first version used id alone — deterministic, under-specified against its own spec, with the test hard-coding an arbitrary winner. A comment now sorts before the `CommentAdded` row that records it: substance before bookkeeping. **A `500` with nothing in it pointing at the cause:** two enum columns stored as `nvarchar` by a value converter, and a `UNION ALL` that aligns branches by column position — only one branch had a real column, the other supplied a literal null EF typed from the CLR enum, and SQL Server returned `nvarchar` where the reader expected `int`. Fixed by projecting `.ToString()` on both sides. **And a sixth entry for the list of tools that lie:** a manual `Invoke-RestMethod` check stored an Arabic comment body as `?????` — the exact signature of `varchar` under a non-Arabic collation, in a brand-new table, so it read as a real schema fault. It was the client: PowerShell 5.1 encodes a string request body as ASCII unless a charset is named. The tell was in the same output, where the author's Arabic name rendered correctly in the *response* from the same console. Settled with a test through `PostAsJsonAsync`, which sends UTF-8. Full evidence in `specs/013-ticket-timeline-and-comments/tests.md`. **One AC is open with an argument and no test: AC-14**, that the query issues no per-entry lookup — the actor name is resolved by a `JOIN` in both branches and no path can loop, but nothing counts round trips, and no test in this suite does for any feature |
| 2026-08-28 | **`008-customer-list-and-profile` backend delivered, and it closed a whole category of unverified criterion.** `GET /api/customers` with search, `GET /api/customers/{id}`, an explicit-collation migration, and the cross-feature **query counter** — 408 tests, 0 warnings, run twice, 30 new. Screens are the frontend lane's | **The feature removed a stub:** `024`'s create-ticket form had a finished customer picker running on hard-coded data, which is why this came before `007` — it makes a built screen work on real data. **The query counter is the part worth reading.** An entire class of criterion had no coverage: every *"this query does not issue one round trip per row"* in the product was met by **reading the LINQ**, which is inspection and cannot see a lazy load, a client-side `ToList` added later, or a projection that stops being translatable after an unrelated edit. `QueryCountingInterceptor` + `QueryCountProbe` were built once, general on purpose, attached through one named seam in `AddInfrastructure`, and used immediately by three tests in three features: `008` AC-11 (new), **`013` AC-14 (which had shipped as *Not claimed*)**, and **`010` AC-12's second half (which had asserted only that the name arrived, not that it arrived in the same query)**. Each asserts the count over a small result **equals** the count over a larger one — the property is "does not grow with the row count", never "is under some number", because a threshold drifts with every unrelated change to the request. **Its most important property is that it refuses to report zero as a pass:** `Count` throws when it observed no commands, because `BeLessThan(3)` is satisfied by zero and an unattached interceptor would have turned all three tests into green no-ops — `001`'s false negative, prevented by design and then verified by removing the seam and watching all three fail with a message naming the two things to check. Negative control 2 added the exact N+1 the `Tickets` count column would have caused, and the counter reported *twelve rows cost 14 round trips and one row cost 3, a difference of 11*. **A measurement reversed this feature's own research.** `research.md` R-2 said `Contains` translates to `LIKE` **without** escaping the term, so AC-8 was written expecting a hand-rolled escaper — and the first implementation had one, which **did not compile**, because `Wasl.Application` cannot see EF Core and `LayerDependencyTests` enforces it. That refusal forced the question to be measured: `search=%` returned 0 of 3 customers, and the command log showed `[FullName] LIKE @search_contains ESCAPE N'\'` — the provider builds the pattern **and escapes the term**. So the hand-rolled escaper would have **double-escaped**, making any customer whose name contained a backslash or a bracket unfindable, and AC-8's test would have passed anyway because it only checks that a pattern character matches nothing extra. Worth naming plainly: **the architecture test prevented a defect it was not written for** — it exists to keep EF Core out of the Application layer, and what it actually did was stop a reimplementation of something the provider already did correctly. R-2's claim is corrected in the handler's remarks rather than left to be rediscovered. **AC-16 was a real defect found by reading, before any code was written:** `001` gave `Email` an explicit CI collation and left `FullName`, `PhoneE164` and `CompanyName` inheriting the database default, and AC-7 searches all three — so **two thirds of the search surface was case-insensitive by luck of the server.** On a `_CS_AS` instance, the default in several installers, searching `ahmed` would silently miss `Ahmed`: identical LINQ, no exception, a smaller result set, nothing looking wrong. Fixed by migration rather than by `COLLATE` in the query, which is non-sargable and would have to be repeated in `015` and `017`; AC-16 reads the collation back from `INFORMATION_SCHEMA` rather than from the configuration that set it. **And one criterion is recorded unmet rather than ticked:** AC-3 wants `400` for a malformed id and the delivered behaviour is `404`, because `{id:guid}` fails the route match before any action runs. Q-A ruled for API consistency — dropping the constraint would leave two resources answering the same malformed input differently, so a client could not write one handler — and `002b` fixes every route at once. `011` met the identical conflict and made the identical choice. Full evidence in `specs/008-customer-list-and-profile/tests.md` |
| 2026-08-28 | **`013` AC-14 and `010` AC-12 are no longer open.** Both were delivered as *argued from the code* and are now measured by `008`'s query counter | Recorded as its own row because two features' evidence changed after they shipped. `013`'s `tests.md` listed AC-14 under *Not claimed* — the actor name is resolved by a `JOIN` in both branches of the timeline union and no path can loop, so it was almost certainly met and nothing proved it. `010` asserted that `customerName` arrived on the row, which a lazy load would also satisfy. Both now have a test that measures the round-trip count over a small result and a larger one and asserts they are equal. **The general lesson is the one the product owner named when approving the utility:** every criterion of this shape in the project was verified by visual inspection, which is not verification — and the fix was one utility rather than four assertions, because four mechanisms measuring the same property is where drift lives (`003` learned the same about audit writers) |
