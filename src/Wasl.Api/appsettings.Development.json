{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "ConnectionStrings": {
    "//": "The compose container from docker-compose.yml, on host port 14330. CHANGED 2026-08-27 — it pointed at Server=.\\SQLEXPRESS, which is a local named instance that exists on one developer's machine. quickstart.md says `docker compose up -d db`, so a clean clone on a Docker-only machine started a container the application never talked to: /health correctly reported the database Unhealthy, and the honest report made it look like a broken health check rather than a wrong address. The password is the same throwaway already committed in docker-compose.yml, so this adds no secret that was not already in the repository — it supersedes `001` AC-10's Windows-auth criterion, which was protecting nothing while making the documented path fail. Point this at a local instance if you have one; nothing else changes.",
    "Wasl": "Server=localhost,14330;Database=Wasl;User Id=sa;Password=Wasl_Local_Dev_2026!;TrustServerCertificate=True;MultipleActiveResultSets=True"
  }
}
